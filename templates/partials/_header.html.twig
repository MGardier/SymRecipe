<!-- Navbar -->
<nav class="navbar navbar-expand-lg  p-3">
	<div class="container-fluid">
		<img href="{{ path('home.index')}}" src="{{ asset('build/img/logo.png') }}" class="mw-25  float-left" height="36">
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class=" collapse navbar-collapse" id="navbarNavDropdown">
			<ul class="navbar-nav ms-auto ">
				<li class="nav-item">
					<a class="nav-link mx-2" aria-current="page" href="{{ path('home.index')}}">Accueil</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('recipe.community')}}">Recettes partagées</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('ingredient.index')}}">Ingrédients</a>
				</li>
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('user.show', {id: app.user.id})}}">Profil</a>
					</li>

					<li class="nav-item dropdown">
						<a class="nav-link mx-2 dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
							{{app.user.fullName }}
						</a>
						<ul class="dropdown-menu p-2" aria-labelledby="navbarDropdownMenuLink">
							{% if 'ROLE_ADMIN' in app.user.roles %}
								<li>
									<a class="dropdown-item " href="{{ path('admin')}}">Administration</a>
								</li>
							{% else %}
								<li>
									<a class="dropdown-item " href="{{ path('user.show', {id: app.user.id})}}">Mon profil</a>
								</li>
								<li>
									<a class="dropdown-item" href="{{ path('recipe.index')}}">Mes recettes</a>
								</li>
								<li>
									<a class="nav-link" href="{{ path('ingredient.index')}}">Mes ingrédients</a>
								</li>
								<li>
									<a class="dropdown-item " href="{{ path('contact.create')}}">Nous contacter</a>
								</li>

								<li>
									<a class="dropdown-item " href="{{ path('security.logout')}}">Déconnexion</a>
								</li>
							{% endif %}
						</ul>
					</li>
				{% else %}
					<li>
						<a class="nav-link" href="{{ path('security.login')}}">
							Connexion
						</a>
					</li>

				{% endif %}
			</ul>
		</div>
	</div>
</nav>
<!-- Navbar -->
