<!--navbar-->
<nav class="navbar navbar-expand-lg bg-primary " data-bs-theme="dark">
	<div class="container-fluid">
		<a class="navbar-brand text-white" href="{{ path('home.index')}}">Symrecipe</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link active text-white" href="{{ path('home.index')}}">Accueil
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link active text-white" href="{{ path('recipe.community')}}">Les recettes de la communauté
					</a>
				</li>
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link active text-white" href="{{ path('ingredient.index')}}">Mes ingrédients
						</a>
					</li>
					<li class="nav-item">
						<a class="nav-link active text-white" href="{{ path('recipe.index')}}">Mes recettes
						</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link active text-white" href="{{ path('security.login')}}">Se connecter
						</a>
					</li>
				{% endif %}
			</ul>
			<div class="d-flex btn">
				<ul class="navbar-nav me-auto">
					{% if app.user %}
						<li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle text-white" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
								{{app.user.fullName }}
							</a>
							<div class="dropdown-menu mt-3" aria-labelledby="navbarDropdown">

								{% if 'ROLE_ADMIN' in app.user.roles %}
									<a class="dropdown-item " href="{{ path('admin')}}">Administration</a>
									<a class="dropdown-item " href="{{ path('user.update.password',{id : app.user.id})}}">Modifier mon mot de passe</a>
								{% else %}
									<a class="dropdown-item " href="{{ path('contact.create')}}">Nous contacter</a>
									<a class="dropdown-item " href="{{ path('user.update',{id : app.user.id})}}">Modifier mes informations</a>
									<a class="dropdown-item " href="{{ path('user.update.password',{id : app.user.id})}}">Modifier mon mot de passe</a>

								{% endif %}

								<div class="dropdown-divider"></div>
								<a class="dropdown-item " href="{{ path('security.logout')}}">Déconnexion</a>
							</div>
						</li>


					{% else %}
						<li class="nav-item">
							<a class="nav-link active text-white btn-inscription" href="{{ path('security.registration')}}">s'inscrire
							</a>
						</li>

					{% endif %}
				</ul>
			</div>
		</div>
	</nav>
	<!-- end navbar-->
